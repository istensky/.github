on:
  push:
    branches: [ $default-branch ]
  pull-reqiest:
   branches: [ $default-branch ]
jobs:
  build-job:
    run-on: ubuntu-latest

    steps:

      - name: step 1
        run:  echo "first step"

      - name: step 2
        run:  echo "second step"
